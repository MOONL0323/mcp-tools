# AI Context System Backend 环境配置示例
# 复制此文件为 .env 并根据实际环境修改配置

# 基础配置
DEBUG=true
ENVIRONMENT=development
SECRET_KEY=your-super-secret-key-change-this-in-production
HOST=0.0.0.0
PORT=8000
WORKERS=4

# 数据库配置
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/ai_context
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=30

# Redis配置
REDIS_URL=redis://localhost:6379/0
REDIS_MAX_CONNECTIONS=20

# JWT认证配置
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
ALGORITHM=HS256

# 文件上传配置
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=104857600  # 100MB
ALLOWED_MIME_TYPES=text/plain,text/markdown,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document

# 安全配置
ALLOWED_HOSTS=*
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# 日志配置
LOG_LEVEL=DEBUG
LOG_FILE=logs/app.log

# 外部服务配置 (可选)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=ai-context-files

# Vector Database 配置
CHROMADB_HOST=localhost
CHROMADB_PORT=8001

# Graph Database 配置
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# 监控配置 (可选)
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000